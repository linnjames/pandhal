<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record model="ir.rule" id="record_quick_sale_purchase">
        <field name="name">Record rule for preferred vendor list</field>
        <field name="model_id" ref="model_preferred_vendor"/>
        <field name="global" eval="True"/>
        <field name="domain_force">[('company_id', 'in', company_ids)]</field>
    </record>
</odoo>