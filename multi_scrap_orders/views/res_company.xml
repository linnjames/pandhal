<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>

        <record id="view_company_form_inherit_multi_scrap_report" model="ir.ui.view">
            <field name="name">res.company.form</field>
            <field name="model">res.company</field>
            <field name="inherit_id" ref="base.view_company_form"/>
            <field name="arch" type="xml">
                <field name="favicon" position="after">
                    <field name="multi_user"/>
                    <field name="mail_ids" widget="many2many_tags"
                           attrs="{'required': [('multi_user', '=',True)],'invisible':[('multi_user','=',False)]}"/>
                </field>
            </field>
        </record>

    </data>
</odoo>
